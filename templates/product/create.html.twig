{% extends "base.html.twig" %}

{# {% form_theme formView 'bootstrap_4_layout.html.twig' %} car on a mit 'boot...' dans config/pack/twig.yaml pour voir ce there partout#}

{% block title %}
	Création d'un product
{% endblock %}

{% block body %}
	<h1>Nouveau produit</h1>

	{# {{ form(formView)}} #}

	{{ form_start(formView)}}

	{{ form_errors(formView)}}

	{#{{ form_widget(formView)}}#}

	{#{{ form_row(formView.name) }}#}

		<div class="row"> <div class="col">
			{{ form_row(formView.name)}}
			{{ form_row(formView.price) }}
		</div>
		<div class="col">
			{{ form_row(formView.mainPicture)}}
			{{ form_row(formView.category) }}
			{{ form_row(formView.shortDescription) }}
		</div>
	</div>

	<button type="submit" class="btn btn-primary">
		<i class="fas fa-save"></i>
		Créer le produit
	</button>

	{{ form_end(formView)}}

{% endblock %}

{# {% block form_row %}
	<div class="form_group">
		{{ form_label(form) }}
		{{ form_widget(form, {'attr': {'class': 'form-control'}})}}
		{{ form_help(form) }}
		{{ form_errors(form) }}
	</div>
{% endblock %} car on a remplace _self par 'bootstrap_4_layout.html.twig' #}
